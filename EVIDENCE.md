# Verification evidence

Generated by `npm run evidence` from repository facts plus the lint, test, and build commands.
Browser tests are listed by spec name and are not executed here.
Parent commit: 96a77c9cad1433560a4c58da675041909c29ecb9
The recorded SHA is the commit the generator ran against. This file is committed afterwards, so the SHA is the parent of the evidence commit. The staleness gate ignores that line.

## Workspaces

package.json workspaces: `packages/*`, `apps/*`
Resolved packages: `@loop/types`, `@loop/api`, `@loop/web`
Node v22.23.2, engines >=22 <23

## Commands and results

Lint exit 0

```text
> lint
> npm run lint --workspaces && eslint scripts tests *.ts *.mjs && node --import tsx scripts/check-vocabulary.ts && node --import tsx scripts/check-prose.ts && node --import tsx scripts/generate-evidence.ts --check
> @loop/types@0.0.0 lint
> eslint src
> @loop/api@0.0.0 lint
> eslint src test
> @loop/web@0.0.0 lint
> eslint app components lib test
Status vocabulary gate: PASS (no mirrored status lists in apps or packages)
Prose gate: PASS (no em dashes in tracked files)
```

Test exit 0. TAP totals: tests 102, pass 102, fail 0, skipped 0.

```text
> test
> npm run build -w @loop/types && npm run test --workspaces
> @loop/types@0.0.0 build
> tsc -p tsconfig.json
> @loop/types@0.0.0 test
> node --import tsx --test test/*.test.ts
# tests 4
# pass 4
# fail 0
# cancelled 0
# skipped 0
# todo 0
> @loop/api@0.0.0 test
> npm run build && node --test dist/test/*.test.js
> @loop/api@0.0.0 build
> tsc -p tsconfig.json
# tests 93
# pass 93
# fail 0
# cancelled 0
# skipped 0
# todo 0
> @loop/web@0.0.0 test
> node --import tsx --test test/*.test.ts
# tests 5
# pass 5
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Build exit 0

```text
> build
> npm run build -w @loop/types && npm run build -w @loop/api && npm run build -w @loop/web
> @loop/types@0.0.0 build
> tsc -p tsconfig.json
> @loop/api@0.0.0 build
> tsc -p tsconfig.json
> @loop/web@0.0.0 build
> next build
▲ Next.js 16.3.4 (Turbopack)
✓ Compiled successfully
  Generating static pages using workers (0/3) ...
✓ Generating static pages using workers (3/3)
Route (app)
┌ ○ /
└ ○ /_not-found
○  (Static)  prerendered as static content
```

## Browser specs (not executed)

tests/agent-command.spec.ts
- a /task finishes with stdout no one hardcoded

tests/agent-routing.spec.ts
- two /task posts route to two different agent commands

tests/auth.spec.ts
- login form then the room after login

tests/echo-runner.spec.ts
- echo runner finishes /task without PATCH and fails FAIL: titles

tests/files.spec.ts
- a member of room A cannot fetch a file belonging to room B by id
- an upload over 10 MB is rejected with 413 and no row is written
- a download response carries attachment and nosniff
- the stored path does not contain the uploaded filename
- LOOP_TASK_FILES lists only that room files and the agent can read one
- Files view shows an uploaded file and a member sees the file card

tests/live-feed.spec.ts
- two tabs receive creates and status changes, then reconnect and replay after API restart

tests/parent-task.spec.ts
- a spawned child shows lineage, stays linked after send-back

tests/proposal-card.spec.ts
- proposal card approves, discusses, and rejects

tests/question-card.spec.ts
- question card answers in the browser and the same task reaches done

tests/result-card.spec.ts
- a finished task can be accepted or rejected and sent back with a note

tests/room-agents.spec.ts
- an owner adds an agent from the catalog and a /task @name routes to it
- a non-owner member gets 403 on add
- an agent registered in room A cannot be addressed from room B
- a duplicate name returns 400

tests/rooms.spec.ts
- GET /rooms omits a room the caller does not belong to
- POST /rooms is transactional: the creator is an owner who can enter
- a message posted in one room does not appear in another
- a newly created room can run /task
- POST /tasks with a foreign roomId is rejected

tests/smoke.spec.ts
- smoke: a chat message appears and a short task finishes

tests/tab-badges.spec.ts
- Chat and Tasks tabs show needs-human and active counts, and hide at zero

tests/team.spec.ts
- owner invites, invitee registers with the token, lands as a member of that room and can post there
- a non-owner member gets 403 on POST invites
- the same token used twice fails the second time
- a token for a@x.com used by b@x.com fails
- GET /rooms/:room/invites response contains neither the raw token nor the hash
- registration with no token still says Registration is closed
- a revoked invite is not valid

tests/wip-log.spec.ts
- an expanded running log follows its tail unless the human scrolled up

## Migrations

- apps/api/migrations/0000_tasks_events.sql
- apps/api/migrations/0001_rooms_messages.sql
- apps/api/migrations/0002_task_run.sql
- apps/api/migrations/0003_task_question.sql
- apps/api/migrations/0004_task_agent.sql
- apps/api/migrations/0005_task_verdict.sql
- apps/api/migrations/0006_task_proposal.sql
- apps/api/migrations/0007_task_parent.sql
- apps/api/migrations/0008_auth.sql
- apps/api/migrations/0009_room_agents.sql
- apps/api/migrations/0010_invites.sql
- apps/api/migrations/0011_files.sql
- apps/api/migrations/0012_room_name.sql

## Event insertion

`insert(events)` call sites: 1
- apps/api/src/bus.ts:20

## Screenshots

- docs/screenshots/agent-routing.png 1440x900
- docs/screenshots/auth.png 1440x1800
- docs/screenshots/files.png 1440x900
- docs/screenshots/parent-task.png 1440x900
- docs/screenshots/proposal-card.png 1440x900
- docs/screenshots/result-card.png 1440x900
- docs/screenshots/room-agents.png 1440x900
- docs/screenshots/room-chat.png 1440x900
- docs/screenshots/tab-badges.png 1440x900
- docs/screenshots/tasks-list.png 1440x900
- docs/screenshots/team.png 1440x900

## Tree notes

The runner loads `agents.json` (`LOOP_AGENTS_PATH`, default repo root) at API start and spawns with `shell: false`.
A room owner picks an agent from that catalog; the command never comes from the request. `task.agentId` is the room mention name, resolved through `room_agents` for that room only, then mapped to the catalog command. An unassigned task uses the first room agent whose catalog id is present. An unknown mention or catalog id is claimed then failed through `store.finish`; a room with no runnable agents fails with a message saying so.
Stdout and stderr stream into the task log. Exit 0 finishes `done` with the last nonempty stdout line; nonzero fails with the last nonempty stderr line.
An agent asks by writing a `LOOP_ASK:` line; the runner parks the task in `needs_input` and reruns the same agent with `LOOP_TASK_ANSWER` after a human answers.
An agent proposes by writing a `LOOP_PROPOSE:` JSON line; the runner parks the task in `needs_input` and reruns with `LOOP_TASK_CHOICE` after a human decides. A malformed proposal fails the task.
An agent spawns a child by writing a `LOOP_SPAWN:` JSON line. The parent keeps running. A malformed spawn is logged on the parent and does not fail it. The child is created through the task store so the runner can claim it next.
A finished task can be accepted or rejected. Reject requeues the same agent, clears the previous run, and passes `LOOP_TASK_NOTE` on the next spawn. `parentTaskId` survives finish and send-back.
The seeded echo agent stays first and keeps the `FAIL:` and `ASK:` title conventions so existing browser specs still pass. A second seeded reviewer agent has a different command.
Claim, progress, finish, ask, answer, review, propose, and decide go through the task store; the bus remains the single `insert(events)` path.
Migration `0002_task_run.sql` adds `claimed_by`, `run_id`, `log`, `result`, and `error`.
Migration `0003_task_question.sql` adds `question`, `answer`, and `answered_by`.
Migration `0004_task_agent.sql` adds nullable `agent_id`.
Migration `0005_task_verdict.sql` adds nullable `verdict`, `verdict_note`, and `verdict_by`.
Migration `0006_task_proposal.sql` adds nullable `proposal`, `proposal_choice`, and `proposal_by`.
Migration `0007_task_parent.sql` adds nullable `parent_task_id` referencing `tasks(id)`.
Migration `0008_auth.sql` adds `principals`, `credentials`, `sessions`, and `room_members`, plus nullable `*_principal_id` columns next to the legacy attribution strings.
Migration `0009_room_agents.sql` adds `room_agents` (catalog id, mention name, no command) unique on `(room_id, name)`, and seeds echo and reviewer into the default room.
Migration `0010_invites.sql` adds `invites` (room, email, role, token hash, invited by, accepted at, expires at) unique on token hash and on pending `(room_id, email)`. An owner copies the invite link; Loop does not send email. Register accepts an optional invite token and joins that room in the same transaction that marks the invite accepted.
Migration `0012_room_name.sql` adds `rooms.name` and backfills the seeded default room as Loop. `GET /rooms` lists memberships only. `POST /rooms` inserts the room row, owner membership, and catalog agents in one transaction.
A global guard requires a live `loop_session` cookie except on register, login, and health. Attribution is stamped from the session, not the request body. Agents get principals at boot and never get a session.
